<UserControl x:Class="Lab4.TextWidget.TextWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab4.TextWidget"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Foreground="DarkGreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Text analysis" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock FontSize="16" Margin="0,5">
                <Run Text="Character count: " FontWeight="SemiBold"/>
                <Run x:Name="CharCountText" Text="0"/>
            </TextBlock>
            <TextBlock FontSize="16" Margin="0,5">
                <Run Text="Word count: " FontWeight="SemiBold"/>
                <Run x:Name="WordCountText" Text="0"/>
            </TextBlock>
        </StackPanel>
        
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReceivedTextBlock" TextWrapping="Wrap" FontSize="14"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
